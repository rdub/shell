# environment variables
. ~/.shell/environment

#aliases
. ~/.shell/aliases

# git functions
. ~/.shell/git_utils

# setup git
git_setup

# e environment manager
if [ -d ~/.e ]; then
	eval "$(~/.e/e.py init)"
fi

# prompt
. ~/.shell/prompt
